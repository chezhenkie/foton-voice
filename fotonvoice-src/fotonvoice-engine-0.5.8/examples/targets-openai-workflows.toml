## targets-openai-workflows.toml - OpenAI-API-powered workflow targets
##
## All targets here use an OpenAI-compatible API for post-processing with different
## models, modes, and custom prompts.  Requires: global openai_enabled = true
## in config.json and an OpenAI-compatible server running at http://localhost:11434.
##
## Copy to ~/.config/voxctl/targets.toml (or merge with an existing file)

format_version = "1.1"

# -- Base inject target (no LLM post-processing) --------------------------------------------
[[target]]
id = "default"
label = "Focused Window"
delivery = "inject"
file_timestamp = true

# -- Grammar fix only (small/fast model) --------------------------------------
[[target]]
id = "grammar_fix"
label = "Grammar Fix"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:1b", openai_mode = "clean"}

# -- Formal tone rewrite (larger model for better results) ---------------------
[[target]]
id = "formal"
label = "Formal Rewrite"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:3b", openai_mode = "formal"}

# -- Casual / friendly tone ---------------------------------------------------
[[target]]
id = "casual"
label = "Casual Rewrite"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:1b", openai_mode = "casual"}

# -- Bullet points for meetings/presentations ---------------------------------
[[target]]
id = "bullets"
label = "Bullet Points"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, auto_format_lists = true, openai_enabled = true, openai_mode = "bullet"}

# -- Make concise (great for Slack/Twitter) -----------------------------------
[[target]]
id = "concise"
label = "Make Concise"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_mode = "concise"}

# -- Domain-specific: software engineering commit messages ---------------------
[[target]]
id = "commit_msg"
label = "Git Commit Message"
delivery = "exec"
command = "wl-copy {TEXT}"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = false, openai_enabled = true, openai_model = "llama3.2:1b", openai_prompt = "Write a concise git commit message for the following description. Use the imperative mood (e.g. 'Add', 'Fix', 'Update'). Output ONLY the commit message, no explanation:\n\n{text}"}

# -- Domain-specific: medical clinical notes ----------------------------------
[[target]]
id = "clinical_notes"
label = "Clinical Notes"
delivery = "file"
file_path = "~/notes/clinical.md"
file_prefix = "- "
file_timestamp = true
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:1b", openai_prompt = "Convert to concise clinical notes using standard medical abbreviations. SOAP format if applicable. Output ONLY the notes:\n\n{text}"}

# -- Domain-specific: legal dictation -----------------------------------------
[[target]]
id = "legal_dict"
label = "Legal Dictation"
delivery = "file"
file_path = "~/notes/legal.md"
file_timestamp = true
processing = {remove_fillers = true, spoken_punctuation = true, auto_format_lists = true, openai_enabled = true, openai_model = "llama3.2:3b", openai_prompt = "Rewrite the following as formal legal language. Use precise, unambiguous terminology. Output ONLY the rewritten text:\n\n{text}"}

# -- Translate to Spanish ------------------------------------------------------
[[target]]
id = "translate_es"
label = "Translate to Spanish"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:1b", openai_prompt = "Translate the following to Spanish. Output ONLY the translation:\n\n{text}"}

# -- Summarise long dictation --------------------------------------------------
[[target]]
id = "summarise"
label = "Summarise"
delivery = "clipboard"
file_timestamp = false
processing = {remove_fillers = true, spoken_punctuation = true, openai_enabled = true, openai_model = "llama3.2:1b", openai_prompt = "Summarise the following in 2-3 sentences. Output ONLY the summary:\n\n{text}"}
